import React from 'react';
import './notFound.css';

export default () => (
  <div className="NotFound">
    <h3>
      Sorry, page not found! Please check the URL or press the App title to
      navigate to home.
    </h3>
  </div>
);
